<h1>My Tasks</h1>

<% @tasks.each do |task|  %>
  <p><%= link_to task.title, task_path(task) %> | <%= link_to "delete", task_path(task), method: :delete, data: { confirm: "Are you sure you want to delete?" }  %></p>
<% end %>

<%= link_to "Add a new task", tasks_new_path %>
